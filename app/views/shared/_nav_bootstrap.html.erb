<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <a class="brand" href="#">CrowFunder</a>
      <div class="nav-collapse collapse">
        <ul class="nav">
          <li class="<%= 'active' if current_page? root_path %>">
            <%= link_to 'Home', :root %>
          </li>
          <li class="<%= 'active' if params[:controller] == 'projects'%>">
            <%= link_to 'Projects', :projects %>
          </li>
          <li class="<%= 'active' if params[:controller] == 'users'%>">
            <% if logged_in? %>
              <%= link_to 'Logout', session_path, method: :delete %>
            <% else %>
              <%= link_to 'Sign up', new_user_path %></li><li>
              <%= link_to 'Sign in', new_session_path%>
            <% end %>
          </li>
          <li class="<%= 'active' if params[:controller] == 'my/projects'%>">
            <%= link_to  "My Projects",my_projects_path %>
          </li>
        </ul>
      </div>
    </div>
  </div>
</div>